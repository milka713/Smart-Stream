services:
  smart-stream:
    build: .
    container_name: smart-stream
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - db-data:/app/data
    environment:
      - DATABASE_URL=sqlite:////app/data/smart_stream.db

volumes:
  db-data:
